import React, {useEffect} from 'react';
import imageIcon from "../../static/img/image-gallery.svg";
import constans from "../../helpers/constants";

const FormType1Boot1 = ({form, handleImageUpload, handleInputUpdate, deleteImg, inputs, images}) => {
    useEffect(() => {
        console.log(form);
    }, [form]);

    return '';
};

export default FormType1Boot1;
